#include <stdio.h>
#include <stdarg.h>

/*Function prototype*/

void print_numbers(const char *separator, const unsigned int n, ...);

/**
 * main: call the function below
 * Return: always 0
 */

int main(void)
{
	print_numners(", ", 4, 0, 98, -1024, 402);
	return (0);
}

/**
 * print_numbers - function that prints numbers followed by a new line
 * @
